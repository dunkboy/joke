eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8760/eureka/
server:
  port: 8763
spring:
  application:
    name: service-ribbon
  profiles:
    active: dev
# 默认未开启断路器
feign:
  hystrix:
    enabled: true